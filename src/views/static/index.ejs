<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ./partials/head.ejs %>
    <link rel="stylesheet" href="/css/index.css">
  </head>
  <body>
    <% include ./partials/navbar.ejs %>
    <header class="jumbotron mb-4">

        <div class="splash-text col-lg-6">
          <h1 class="display-3">Welcome to Barkipedia</h1>
          <p class="lead">Create, share, and collaborate with canine wiki pages</p>
        </div>
        <% if (currentUser) { %>
          <div class="col-lg-1 pt-2">
            <a href="/wikis"><button type="button" class="btn btn-lg btn-primary">Explore!</button></a>
          </div>
        <% } %>
    </header>
      <main class="container">
        <% include ../static/partials/messages.ejs %> 
        
        <div class="features row my-4 text-center">
            <div class="col-lg-4 mx-auto">
              <h1 class="pb-4">Share knowledge</h1>
              <p>The world can be a scary place for our four-legged friends. Learn the best techniques and tips from others passionate about dogs!</p>
            </div>
            <div class="col-lg-4 mx-auto">
              <h1 class="pb-4">Markdown Editing</h1>
              <p>Add rich formatting to wikis with full Markdown support, including images!</p>
            </div>
            <div class="col-lg-4 mx-auto">
              <h1 class="pb-4">Private Wikis</h1>
              <p>Have a project with sensitive info? Create wikis with restricted user access.</p>
            </div>
        </div>
        <div class="card-deck mb-3 text-center">
            <% if (!currentUser) { %>
            <div class="card mb-4 shadow-sm">
              <div class="card-header">
                <h4 class="my-0 font-weight-normal">Standard</h4>
              </div>
              <div class="card-body">
                <h1 class="card-title pricing-card-title">$0 <small class="text-muted">/ yr</small></h1>
                <ul class="list-unstyled mt-3 mb-4">
                  <li>Browse and create public wikis</li>
                  <li>Contribute to public wikis</li>
                </ul>
                <a href="/users/sign_up"><button type="button" class="btn btn-lg btn-block btn-outline-primary">Sign up for free</button></a>
              </div>
            </div>
            <% } %>
            <% if (!currentUser || !currentUser.isPremium()) { %>
            <div class="card mb-4 shadow-sm">
              <div class="card-header">
                <h4 class="my-0 font-weight-normal">Premium</h4>
              </div>
              <div class="card-body">
                <h1 class="card-title pricing-card-title">$15 <small class="text-muted">/ yr</small></h1>
                <ul class="list-unstyled mt-3 mb-4">
                  <li>Create private wikis</li>
                  <li>Add/remove collaborators to private wikis</li>
                </ul>
                <% if (currentUser && !currentUser.isPremium()) { %>
                  <a href="/users/upgrade"><button type="button" class="btn btn-lg btn-block btn-primary">Get started</button></a>
                  <% } else { %>
                  <a href="/users/sign_up"><button type="button" class="btn btn-lg btn-block btn-primary">Get started</button></a>
                <% } %>
              </div>
            </div>
            <% } %>
          </div>
          
      </main>
      <% include ./partials/foot.ejs %>
      <% include ./partials/baseScripts.ejs %>
    </body>
</html>
